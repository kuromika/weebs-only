extends layout

block content
  div(class='flex flex-col items-center justify-center gap-5')
    img(src='/images/error.jpg' class='rounded-lg')
    div(class='font-bold text-center')
      h2= message
      h3= error.status
    pre #{error.stack}
